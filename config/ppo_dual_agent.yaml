behaviors:
  DDAAgent:
    trainer_type: ppo
    hyperparameters:
      batch_size: 64           # Increased batch size for stable policy updates
      buffer_size: 512         # Larger buffer size for representative PPO experience
      learning_rate: 3.0e-4
      beta: 1.0e-3             # Lower entropy to accelerate policy exploitation
      epsilon: 0.2
      lambd: 0.95
      num_epoch: 4
      learning_rate_schedule: linear
    network_settings:
      normalize: true
      hidden_units: 128        # Small network for 10 observations
      num_layers: 2
      vis_encode_type: simple
    reward_signals:
      extrinsic:
        gamma: 0.99
        strength: 1.0
    keep_checkpoints: 5
    max_steps: 50100           # Set slightly above our 50,010 checkpoint to trigger clean completion
    time_horizon: 10           # 10 game episodes = 1 meta-episode
    summary_freq: 100
